# Bridges

Exclusive for bridging (connecting) different systems together. A bridge node is
a single node that takes two or more system nodes as setup arguments, and
applies any bridgin and custom logic necessary to make these systems interact
with each other.

Doing this way allows for systems to be modular and reusable and avoids
hardcoding dependencies on other systems.
